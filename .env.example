# TRMNL Weather Radar Plugin - Environment Variables
# Copy this file to .env and configure as needed

# IP Whitelist (Optional)
# Enable IP whitelisting to restrict access to TRMNL servers only
# Set to 'true' to enable, 'false' to disable (default: false)
# When enabled, only requests from TRMNL IPs will be accepted
ENABLE_IP_WHITELIST=false

# Note: No API keys required! Open-Meteo is completely free.